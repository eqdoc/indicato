# Reference

Every function in `indicato`, generated from the source. Do not edit this file —
run `node scripts/build-docs.mjs` instead.

**89 functions.** The confidence column says how firmly each one is pinned down;
[accuracy notes](./accuracy.md) has the detail.

That document counts 73 verified *series* where this table counts 58 verified
*functions* — not a contradiction. A function like `dmi` returns three series
(+DI, −DI and ADX) and each was checked separately.

| status | meaning |
|---|---|
| ✅ exact | pinned to the value, across 6,126 bars of real market history |
| 🟡 scaled | pinned to the shape; a documented scaling or parameter convention differs |
| ⚪ by definition | follows its published formula, held by the property tests |
| 🔴 ambiguous | the indicator itself is under-specified and implementations diverge |

## All of them

| function | signature | status |
|---|---|---|
| [`ac`](#ac) | `(bars)` | ✅ exact |
| [`adl`](#adl) | `(bars)` | ✅ exact |
| [`alligator`](#alligator) | `(bars)` | 🟡 scaled |
| [`alma`](#alma) | `(src, n, offset = 0.85, sigma = 6)` | ✅ exact |
| [`ao`](#ao) | `(bars)` | ✅ exact |
| [`aroon`](#aroon) | `(bars, n = 25)` | ✅ exact |
| [`atr`](#atr) | `(bars, n = 14)` | ✅ exact |
| [`bbPercent`](#bbPercent) | `(bars, n = 20, mult = 2)` | ✅ exact |
| [`bbWidth`](#bbWidth) | `(bars, n = 20, mult = 2)` | 🟡 scaled |
| [`beta`](#beta) | `(a, b, n = 20)` | ⚪ by definition |
| [`bollinger`](#bollinger) | `(bars, n = 20, mult = 2)` | ✅ exact |
| [`bop`](#bop) | `(bars)` | ✅ exact |
| [`cci`](#cci) | `(bars, n = 20)` | ✅ exact |
| [`chaikinOsc`](#chaikinOsc) | `(bars, fast = 3, slow = 10)` | ✅ exact |
| [`chandeKrollStop`](#chandeKrollStop) | `(bars, p = 10, x = 1, q = 9)` | ⚪ by definition |
| [`chandelierExit`](#chandelierExit) | `(bars, n = 22, mult = 3)` | 🟡 scaled |
| [`chop`](#chop) | `(bars, n = 14)` | ✅ exact |
| [`cmf`](#cmf) | `(bars, n = 20)` | ✅ exact |
| [`cmo`](#cmo) | `(src, n = 14)` | ✅ exact |
| [`coppock`](#coppock) | `(src, a = 14, b = 11, n = 10)` | ✅ exact |
| [`correlation`](#correlation) | `(a, b, n = 20)` | ⚪ by definition |
| [`crsi`](#crsi) | `(bars, rsiLen = 3, streakLen = 2, rankLen = 100)` | 🟡 scaled |
| [`dema`](#dema) | `(src, n)` | ✅ exact |
| [`dmi`](#dmi) | `(bars, n = 14)` | ✅ exact |
| [`donchian`](#donchian) | `(bars, n = 20)` | ⚪ by definition |
| [`dpo`](#dpo) | `(src, n = 20)` | ✅ exact |
| [`elderRay`](#elderRay) | `(bars, n = 13)` | ✅ exact |
| [`ema`](#ema) | `(values, n)` | ✅ exact |
| [`emv`](#emv) | `(bars, n = 14)` | 🟡 scaled |
| [`envelopes`](#envelopes) | `(bars, n = 20, pct = 2.5)` | ⚪ by definition |
| [`fisher`](#fisher) | `(bars, n = 9)` | 🟡 scaled |
| [`forceIndex`](#forceIndex) | `(bars, n = 13)` | ✅ exact |
| [`frama`](#frama) | `(bars, n = 16)` | ⚪ by definition |
| [`gator`](#gator) | `(bars)` | ⚪ by definition |
| [`historicalVolatility`](#historicalVolatility) | `(bars, n = 20, barsPerYear = 252)` | ⚪ by definition |
| [`hma`](#hma) | `(src, n)` | ✅ exact |
| [`ichimoku`](#ichimoku) | `(bars, conv = 9, base = 26, spanB = 52)` | ⚪ by definition |
| [`kama`](#kama) | `(src, n = 10, fast = 2, slow = 30)` | ✅ exact |
| [`keltner`](#keltner) | `(bars, n = 20, mult = 2)` | ⚪ by definition |
| [`klinger`](#klinger) | `(bars, fast = 34, slow = 55, sig = 13)` | 🔴 ambiguous |
| [`kst`](#kst) | `(src)` | 🟡 scaled |
| [`linreg`](#linreg) | `(src, n = 14)` | ✅ exact |
| [`linregSlope`](#linregSlope) | `(src, n = 14)` | ✅ exact |
| [`macd`](#macd) | `(bars, fast = 12, slow = 26, signal = 9)` | ✅ exact |
| [`massIndex`](#massIndex) | `(bars, n = 25, e = 9)` | ✅ exact |
| [`mcginley`](#mcginley) | `(src, n = 14)` | ✅ exact |
| [`median`](#median) | `(src, n = 14)` | ✅ exact |
| [`mfi`](#mfi) | `(bars, n = 14)` | ✅ exact |
| [`mode`](#mode) | `(src, n = 20, dp = 1)` | ⚪ by definition |
| [`momentum`](#momentum) | `(src, n = 10)` | ✅ exact |
| [`natr`](#natr) | `(bars, n = 14)` | ✅ exact |
| [`nvi`](#nvi) | `(bars)` | 🟡 scaled |
| [`obv`](#obv) | `(bars)` | ✅ exact |
| [`pivotPoints`](#pivotPoints) | `(bars)` | ⚪ by definition |
| [`po`](#po) | `(src, fast = 12, slow = 26)` | ✅ exact |
| [`ppo`](#ppo) | `(src, fast = 12, slow = 26)` | ✅ exact |
| [`psar`](#psar) | `(bars, step = 0.02, max = 0.2)` | 🟡 scaled |
| [`pvi`](#pvi) | `(bars)` | 🟡 scaled |
| [`pvt`](#pvt) | `(bars)` | ✅ exact |
| [`roc`](#roc) | `(src, n = 12)` | ✅ exact |
| [`rsi`](#rsi) | `(bars, n = 14)` | ✅ exact |
| [`rvi`](#rvi) | `(bars, n = 10)` | ✅ exact |
| [`rwi`](#rwi) | `(bars, n = 14)` | 🟡 scaled |
| [`sma`](#sma) | `(values, n)` | ✅ exact |
| [`starc`](#starc) | `(bars, n = 15, atrLen = 15, mult = 2)` | ⚪ by definition |
| [`stc`](#stc) | `(src, fast = 23, slow = 50, cycle = 10)` | ✅ exact |
| [`stochastic`](#stochastic) | `(bars, n = 14, k = 3, d = 3)` | ✅ exact |
| [`stochRsi`](#stochRsi) | `(bars, n = 14, k = 3, d = 3)` | 🟡 scaled |
| [`supertrend`](#supertrend) | `(bars, n = 10, mult = 3)` | ✅ exact |
| [`t3`](#t3) | `(src, n = 5, v = 0.7)` | ✅ exact |
| [`tema`](#tema) | `(src, n)` | ✅ exact |
| [`trima`](#trima) | `(src, n)` | ✅ exact |
| [`trix`](#trix) | `(src, n = 15)` | ✅ exact |
| [`tsf`](#tsf) | `(src, n = 14)` | ✅ exact |
| [`tsi`](#tsi) | `(src, long = 25, short = 13)` | ✅ exact |
| [`ulcerIndex`](#ulcerIndex) | `(bars, n = 14)` | ✅ exact |
| [`ultosc`](#ultosc) | `(bars, s = 7, m = 14, l = 28)` | ✅ exact |
| [`variance`](#variance) | `(src, n = 14)` | ✅ exact |
| [`vidya`](#vidya) | `(src, n = 14, smooth = 20)` | 🟡 scaled |
| [`volumeOsc`](#volumeOsc) | `(bars, fast = 5, slow = 10)` | 🟡 not comparable |
| [`vortex`](#vortex) | `(bars, n = 14)` | ✅ exact |
| [`vroc`](#vroc) | `(bars, n = 14)` | ✅ exact |
| [`vwap`](#vwap) | `(bars)` | ⚪ by definition |
| [`vwma`](#vwma) | `(bars, n)` | ✅ exact |
| [`williamsFractals`](#williamsFractals) | `(bars, w = 2)` | ⚪ by definition |
| [`williamsR`](#williamsR) | `(bars, n = 14)` | ✅ exact |
| [`wma`](#wma) | `(values, n)` | ✅ exact |
| [`yearBand`](#yearBand) | `(bars, n = 252)` | ⚪ by definition |
| [`zlema`](#zlema) | `(src, n)` | ✅ exact |

---

## ac

```js
import { ac } from "indicato";

ac(bars)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## adl

```js
import { adl } from "indicato";

adl(bars)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Accumulation/Distribution — Chaikin's money flow multiplier, accumulated.

---

## alligator

```js
import { alligator } from "indicato";

alligator(bars)
```

**🟡 scaled** — Ratio 0.997, correlation 0.9995 against pandas-ta — a smoothed-average variant.

Bill Williams' Alligator: three displaced smoothed averages. The displacement is FORWARD, which on a chart is a line drawn ahead of price — so for a signal each is read from its own past, never from a bar that has not happened.

---

## alma

```js
import { alma } from "indicato";

alma(src, n, offset, sigma)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Arnaud Legoux — a gaussian window offset toward the recent end.

---

## ao

```js
import { ao } from "indicato";

ao(bars)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Awesome and Accelerator — Bill Williams, on the bar's midpoint.

---

## aroon

```js
import { aroon } from "indicato";

aroon(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Aroon — Chande. How long since the window's high and low, as a percentage. Both at 100 means the extreme is today.

---

## atr

```js
import { atr } from "indicato";

atr(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Exported since Keltner needs it and a reader may want it on its own: the size of a typical session, in rupees. TRUE range, not high minus low — a share that gapped 100 to 130 overnight then traded 130-132 had a 2-rupee day by high minus low and a 32-rupee day in truth.

---

## bbPercent

```js
import { bbPercent } from "indicato";

bbPercent(bars, n, mult)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Bollinger %B and bandwidth — where price sits in the band, and how wide it is.

---

## bbWidth

```js
import { bbWidth } from "indicato";

bbWidth(bars, n, mult)
```

**🟡 scaled** — ×100 against the OpenAlgo SDK — a percentage here, a fraction there. Percentage matches the TradingView convention.

---

## beta

```js
import { beta } from "indicato";

beta(a, b, n)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

---

## bollinger

```js
import { bollinger } from "indicato";

bollinger(bars, n, mult)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## bop

```js
import { bop } from "indicato";

bop(bars)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Balance of Power — where the close finished relative to the open, scaled by range.

---

## cci

```js
import { cci } from "indicato";

cci(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Commodity Channel Index — Donald Lambert. The 0.015 scales it so roughly 70-80% of readings fall within ±100; it is his, not a tuning knob.

---

## chaikinOsc

```js
import { chaikinOsc } from "indicato";

chaikinOsc(bars, fast, slow)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## chandeKrollStop

```js
import { chandeKrollStop } from "indicato";

chandeKrollStop(bars, p, x, q)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

Chande Kroll stop — the same idea with a two-stage lookback.

---

## chandelierExit

```js
import { chandelierExit } from "indicato";

chandelierExit(bars, n, mult)
```

**🟡 scaled** — Ratio 0.976 against pandas-ta — a different ATR variant inside the stop.

Chandelier Exit — Le Beau. A stop hung from the highest high since entry.

---

## chop

```js
import { chop } from "indicato";

chop(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Choppiness — Dreiss. 100 means directionless, 0 means a clean trend.

---

## cmf

```js
import { cmf } from "indicato";

cmf(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Chaikin Money Flow, and the Chaikin Oscillator over the A/D line.

---

## cmo

```js
import { cmo } from "indicato";

cmo(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Chande Momentum Oscillator — RSI keeping its sign, so it swings -100..100.

WILDER-SMOOTHED, not a plain window sum. The first version here summed raw up and
down moves over n bars, which is a fair reading of Chande's description and is not
what anyone else computes: TA-Lib smooths them the way RSI does, which makes
CMO exactly 2*RSI - 100. Verified against TA-Lib to 0.000000000 on 6,126 bars.
A CMO that disagrees with every chart a reader has ever seen is a wrong CMO,
whatever the book says.

---

## coppock

```js
import { coppock } from "indicato";

coppock(src, a, b, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Coppock Curve — a 10-period WMA of two rates of change. Built for monthly bars.

---

## correlation

```js
import { correlation } from "indicato";

correlation(a, b, n)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

Rolling correlation and beta of the close against its own lagged self — the two-series versions need a benchmark the chart does not carry.

---

## crsi

```js
import { crsi } from "indicato";

crsi(bars, rsiLen, streakLen, rankLen)
```

**🟡 scaled** — 1.3% from the OpenAlgo SDK, correlation 1.0000 — the rank component's window edge.

Connors RSI — Connors and Alvarez. Three ideas averaged: a very short RSI, an RSI of the streak of consecutive up or down closes, and where today's return ranks against the last hundred. Built for mean reversion over two or three days.

---

## dema

```js
import { dema } from "indicato";

dema(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Double and triple exponential — Patrick Mulloy, 1994. Subtracting the lag of an EMA of an EMA is the whole trick.

---

## dmi

```js
import { dmi } from "indicato";

dmi(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## donchian

```js
import { donchian } from "indicato";

donchian(bars, n)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

---

## dpo

```js
import { dpo } from "indicato";

dpo(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Detrended Price Oscillator — price against an average displaced back by half the period, which removes the trend rather than measuring it.

---

## elderRay

```js
import { elderRay } from "indicato";

elderRay(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Elder Ray — how far buyers and sellers pushed past the trend.

---

## ema

```js
import { ema } from "indicato";

ema(values, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## emv

```js
import { emv } from "indicato";

emv(bars, n)
```

**🟡 scaled** — ×10⁴ against the OpenAlgo SDK — both scale an arbitrary ratio and neither scaling is canonical.

Ease of Movement — Arms. How far price moved per unit of volume.

---

## envelopes

```js
import { envelopes } from "indicato";

envelopes(bars, n, pct)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

Moving-average envelopes — a fixed percentage either side.

---

## fisher

```js
import { fisher } from "indicato";

fisher(bars, n)
```

**🟡 scaled** — Correlation 0.944 against the OpenAlgo SDK — Ehlers published 0.66/0.67 smoothing constants, other implementations use different ones.

Fisher Transform — Ehlers. Forces a bounded series toward a gaussian shape so turns become sharp rather than gradual.

---

## forceIndex

```js
import { forceIndex } from "indicato";

forceIndex(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Force Index — Elder. Price change times volume.

---

## frama

```js
import { frama } from "indicato";

frama(bars, n)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

Ehlers' fractal-adaptive — measures the price path's fractal dimension over the window and lets that set the EMA's alpha. A straight run gives D near 1 and a fast average; a jagged one gives D near 2 and a slow one.

---

## gator

```js
import { gator } from "indicato";

gator(bars)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

---

## historicalVolatility

```js
import { historicalVolatility } from "indicato";

historicalVolatility(bars, n, barsPerYear)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

Historical volatility — the annualised standard deviation of log returns.

THE ANNUALISATION DEPENDS ON YOUR BAR SIZE and this is the one place a timeframe
assumption can be silently wrong. 252 is the count of trading DAYS in a year, so the
default is right for daily bars and wrong for every other size: pass 52 for weekly, 12
for monthly, or roughly 1,575 for hourly bars on a 6.25-hour Indian session. Getting
this wrong does not error — it scales the answer by the square root of the ratio.

---

## hma

```js
import { hma } from "indicato";

hma(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Hull — Alan Hull. WMA(2*WMA(n/2) - WMA(n), sqrt(n)). Fast and smooth at once, at the cost of overshooting turns.

---

## ichimoku

```js
import { ichimoku } from "indicato";

ichimoku(bars, conv, base, spanB)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

Ichimoku, drawn without the forward shift. The cloud is normally plotted 26 sessions into the future, and a chart that ends today would then end 26 bars of empty space to the right of the last candle. The two spans are drawn at the bar they were computed on, which is the honest version of the same lines.

---

## kama

```js
import { kama } from "indicato";

kama(src, n, fast, slow)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Kaufman's adaptive — speeds up in a trend, slows in chop, via an efficiency ratio of net travel over total travel.

---

## keltner

```js
import { keltner } from "indicato";

keltner(bars, n, mult)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

Keltner channel: an EMA with ATR-width rails. Bollinger's bands widen with standard deviation, Keltner's with true range — which is why traders watch the pair together, and why shipping one without the other was a gap.

---

## klinger

```js
import { klinger } from "indicato";

klinger(bars, fast, slow, sig)
```

**🔴 ambiguous** — Correlation 0.92 against pandas-ta with a ratio of 47, and it does not match their signal line either. Klinger's original description is ambiguous about the cumulative-measurement logic and implementations genuinely diverge. Treat as unverified.

Klinger — Kroll's volume force, and Price Volume Trend.

---

## kst

```js
import { kst } from "indicato";

kst(src)
```

**🟡 scaled** — ×0.01 against pandas-ta — they report a fraction where this reports Pring's scaled value.

Know Sure Thing — Pring. Four smoothed rates of change, weighted 1..4.

---

## linreg

```js
import { linreg } from "indicato";

linreg(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## linregSlope

```js
import { linregSlope } from "indicato";

linregSlope(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## macd

```js
import { macd } from "indicato";

macd(bars, fast, slow, signal)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---- the families ----

---

## massIndex

```js
import { massIndex } from "indicato";

massIndex(bars, n, e)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Mass Index — Dorsey. Range expansion as a reversal warning.

---

## mcginley

```js
import { mcginley } from "indicato";

mcginley(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

McGinley Dynamic — John McGinley. Divides the step by a speed factor so the line tracks price instead of trailing it through a fast move.

---

## median

```js
import { median } from "indicato";

median(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Rolling median and variance of the close.

---

## mfi

```js
import { mfi } from "indicato";

mfi(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## mode

```js
import { mode } from "indicato";

mode(src, n, dp)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

Rolling mode, to one decimal. Genuinely rare in trading and included for completeness: on continuous prices almost every value is unique, so it reports the most common ROUNDED price in the window — which is a crude support/resistance read and nothing more.

---

## momentum

```js
import { momentum } from "indicato";

momentum(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## natr

```js
import { natr } from "indicato";

natr(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Normalised ATR — volatility as a percentage of price, so it compares across stocks.

---

## nvi

```js
import { nvi } from "indicato";

nvi(bars)
```

**🟡 scaled** — Seeds at 1000, per Fosback. Other libraries seed at 0 or 100 and accumulate differently.

---

## obv

```js
import { obv } from "indicato";

obv(bars)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## pivotPoints

```js
import { pivotPoints } from "indicato";

pivotPoints(bars)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

Classic floor-trader pivots from the previous bar.

---

## po

```js
import { po } from "indicato";

po(src, fast, slow)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## ppo

```js
import { ppo } from "indicato";

ppo(src, fast, slow)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Percentage Price Oscillator, and its absolute-difference sibling.

---

## psar

```js
import { psar } from "indicato";

psar(bars, step, max)
```

**🟡 scaled** — Differs on 24 bars of 5,826, median difference 0.0000 — all at a reversal, where a new leg's starting direction is an implementation choice.

Wilder's Parabolic SAR, acceleration 0.02 stepping to 0.20. Written out in full rather than approximated: the whole content of the indicator is where it flips, and a flip one bar early is the only thing a reader would check.

---

## pvi

```js
import { pvi } from "indicato";

pvi(bars)
```

**🟡 scaled** — Seeds at 1000, per Fosback. The OpenAlgo SDK seeds at 100.

---

## pvt

```js
import { pvt } from "indicato";

pvt(bars)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## roc

```js
import { roc } from "indicato";

roc(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Rate of change, and momentum's plain difference.

---

## rsi

```js
import { rsi } from "indicato";

rsi(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

WILDER'S RSI, seeded one bar earlier than TA-Lib.

Measured against TA-Lib 0.7.1 on 6,126 bars: we emit a first value at bar n-1 where it
emits at bar n, and Wilder's smoothing carries that seed forward — 0.65 apart at bar
26, 0.08 by bar 50, 0.002 by bar 100, and EXACTLY zero from bar 300 on. No backtest
here is affected, because none of them trades inside the warmup. It is written down
because a library that is silently a hair different from the reference for its first
fifty bars is a library somebody will file an issue about, and they would be right.

---

## rvi

```js
import { rvi } from "indicato";

rvi(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Relative Vigor Index — Dorsey. Where the close finished within the bar, smoothed, on the theory that a rising market closes above its open.

---

## rwi

```js
import { rwi } from "indicato";

rwi(bars, n)
```

**🟡 scaled** — Two unsigned lines here; the OpenAlgo SDK returns one signed line.

Random Walk Index — Poulos. How far price travelled against how far a random walk of the same volatility would be expected to.

---

## sma

```js
import { sma } from "indicato";

sma(values, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## starc

```js
import { starc } from "indicato";

starc(bars, n, atrLen, mult)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

STARC bands — Stoller. A simple average with Wilder ATR either side.

---

## stc

```js
import { stc } from "indicato";

stc(src, fast, slow, cycle)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Schaff Trend Cycle — Schaff. A stochastic applied twice to the MACD line, which makes a slow trend indicator turn like a fast one.

---

## stochastic

```js
import { stochastic } from "indicato";

stochastic(bars, n, k, d)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

The plain stochastic — where the close sits inside the recent high-low range. Not the same thing as the Stochastic RSI already in this file, which runs the same formula over RSI instead of over price; the file had the derivative and not the original.

---

## stochRsi

```js
import { stochRsi } from "indicato";

stochRsi(bars, n, k, d)
```

**🟡 scaled** — Returns the smoothed %K a chart draws; TA-Lib's STOCHRSI returns the raw fastk. Computed the same way they are identical.

STOCHASTIC RSI. This returns the SMOOTHED %K and its %D — the pair a chart draws. TA-Lib's STOCHRSI returns the raw, unsmoothed fastk, which is our %K before its 3-period average; computed that way the two are identical to 0.000000. Both are right and they are different outputs, so the difference is named here rather than found by someone comparing one against the other.

---

## supertrend

```js
import { supertrend } from "indicato";

supertrend(bars, n, mult)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## t3

```js
import { t3 } from "indicato";

t3(src, n, v)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Tillson T3 — six chained EMAs with a volume-factor weighting.

---

## tema

```js
import { tema } from "indicato";

tema(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## trima

```js
import { trima } from "indicato";

trima(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Triangular — a simple average of a simple average, weighting the middle.

---

## trix

```js
import { trix } from "indicato";

trix(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

TRIX — the percentage change of a triple-smoothed EMA.

---

## tsf

```js
import { tsf } from "indicato";

tsf(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## tsi

```js
import { tsi } from "indicato";

tsi(src, long, short)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

True Strength Index — Blau. Double-smoothed momentum over double-smoothed absolute momentum.

---

## ulcerIndex

```js
import { ulcerIndex } from "indicato";

ulcerIndex(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Ulcer Index — Martin. Depth and duration of drawdown, which is closer to what holding something actually feels like than standard deviation is.

---

## ultosc

```js
import { ultosc } from "indicato";

ultosc(bars, s, m, l)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Ultimate Oscillator — Williams. Three horizons weighted 4:2:1, so a single timeframe cannot dominate it.

---

## variance

```js
import { variance } from "indicato";

variance(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## vidya

```js
import { vidya } from "indicato";

vidya(src, n, smooth)
```

**🟡 scaled** — Ratio 0.998 against pandas-ta — their alpha default differs from Chande's CMO-driven one.

Chande's VIDYA — an EMA whose smoothing scales with the CMO, so it moves when direction is one-sided and stalls when it is not.

---

## volumeOsc

```js
import { volumeOsc } from "indicato";

volumeOsc(bars, fast, slow)
```

**🟡 not comparable** — The classic SMA-based oscillator. pandas-ta ships the Percentage Volume Oscillator, which is EMA-based — a different indicator, not a different answer.

Volume oscillator and volume rate of change.

---

## vortex

```js
import { vortex } from "indicato";

vortex(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Vortex — Botes and Siepman. Two lines crossing marks a trend change.

---

## vroc

```js
import { vroc } from "indicato";

vroc(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

---

## vwap

```js
import { vwap } from "indicato";

vwap(bars)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

VWAP — running, from the first bar. Intraday VWAP resets daily; on daily bars there is no session to reset on, so this is the cumulative one and is labelled so.

---

## vwma

```js
import { vwma } from "indicato";

vwma(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Volume-weighted: each bar counts for what traded on it.

---

## williamsFractals

```js
import { williamsFractals } from "indicato";

williamsFractals(bars, w)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

Williams Fractals — a high with two lower highs either side. Confirmed only two bars later, which is the whole point: it cannot be known sooner.

---

## williamsR

```js
import { williamsR } from "indicato";

williamsR(bars, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Williams %R — where the close sits in the range, as a negative percentage.

---

## wma

```js
import { wma } from "indicato";

wma(values, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Weighted moving average: the newest bar counts n times as much as the oldest, weights 1..n over the window. Between SMA (every bar equal) and EMA (weights decay forever), which is the whole reason people ask for it by name.

---

## yearBand

```js
import { yearBand } from "indicato";

yearBand(bars, n)
```

**⚪ by definition** — Follows its published formula. Held by the property tests — causality, warmup, range and flat-series identity.

The rolling high and low over a window of BARS, defaulting to 252 — a trading year of daily sessions. On weekly bars 252 is five years, not one, so pass the window you mean. The name says "year" because that is what it is used for; the maths counts bars.

Excludes the current bar, deliberately: a level computed including today is a level
today cannot exceed, so "makes a new 52-week high" would be false on every bar of every
stock, for ever, and silently.
days, so a calendar year would quietly be a fourteen-month high.

---

## zlema

```js
import { zlema } from "indicato";

zlema(src, n)
```

**✅ exact** — Pinned to the value across 6,126 bars of real market history.

Zero-lag — Ehlers. An EMA fed price plus its own recent change.

---

